Dependencies
	pymongo [to store the products]
	cherrypy [framework to build the web service]
	forencode [for validating the data fields]
	
config.py
	contails the locale and mongodb settings
	
db.py
	contains the code for loading sample data
	
Note: I have loaded the sample data in the database before creating the service